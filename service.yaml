apiVersion: v1
kind: Service
metadata:
  name: dev-service
  namespace: app
  
spec:
  selector:
    app: dev-pod
  ports:
    - protocol: TCP
      port: 8080
      targetPort: app-port
      nodePort: 30010
  type: NodePort
